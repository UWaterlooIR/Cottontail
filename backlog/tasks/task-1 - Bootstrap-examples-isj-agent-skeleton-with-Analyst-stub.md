---
id: TASK-1
title: Bootstrap examples/isj-agent/ skeleton with Analyst stub
status: To Do
assignee: []
created_date: '2026-06-16 17:14'
labels: []
dependencies: []
references:
  - docs/agentic-isj-investigation-planner.md
ordinal: 1000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The agentic-ISJ system specified in [docs/agentic-isj-investigation-planner.md](../docs/agentic-isj-investigation-planner.md) needs a place in the repo. This task creates the full directory skeleton under `examples/isj-agent/` (sibling to the existing `examples/agent/`, which stays untouched), and stubs out only the **Analyst** agent as the first concrete module. Every other agent, engine module, fixture, and test is left for follow-on tasks.

## Full target layout (entire planned skeleton)

The complete tree is shown so future tasks can be scoped against it. **Only items marked `← stubbed in this task` get content in this task. Everything else is either a `.gitkeep` placeholder, a Python `__init__.py` package marker, or simply not created yet.**

```
examples/isj-agent/
  README.md                          ← stubbed (pointer to spec + how-to-run placeholder)
  pyproject.toml                     ← stubbed (own uv project; deps: pydantic, openai)
  uv.lock                            ← stubbed (generated by uv sync)

  prompts/
    analyst.md                       ← stubbed (first-draft Analyst prompt from §4.1.1)
    (cartographer.md, strategist.md, query_author.md, judge.md,
     tactician_reformulate.md, tactician_place_concept.md,
     critic.md, writer.md            ← follow-on tasks)

  isj_agent/
    __init__.py                      ← stubbed (package marker)

    protocol/                        ← typed artifacts (spec §2)
      __init__.py                    ← stubbed (package marker)
      inp.py                         ← stubbed (INP pydantic schema, §2.1)
      (cm.py, ip.py, task_result.py, ranked_list.py
                                     ← follow-on tasks)

    agents/                          ← LLM-role wrappers (§4.1)
      __init__.py                    ← stubbed (package marker)
      analyst.py                     ← stubbed (Analyst stub, §4.1.1)
      (cartographer.py, strategist.py, query_author.py, judge.py,
       tactician.py, critic.py, writer.py
                                     ← follow-on tasks)

    engine/                          ← deterministic layer (§4.2)
      __init__.py                    ← stubbed (package marker)
      (searcher.py, session_controller.py, bookkeeper.py,
       compiler.py, validator.py     ← follow-on tasks)

    (orchestrator.py, cli.py         ← follow-on tasks)

  tests/
    __init__.py                      ← stubbed (package marker)
    test_analyst.py                  ← stubbed (contract-surface test)
    (test_protocol.py, test_cartographer.py, ...
                                     ← follow-on tasks)

    fixtures/
      .gitkeep                       ← stubbed (otherwise-empty directory)
      (ankle_brace_at/, germany/, airports/, creativity/, healthcare/, hinton/
                                     ← per-topic fixtures in follow-on tasks)
```

## What this task delivers

The items marked `← stubbed in this task` above. Specifically:

1. **`README.md`** — short pointer to `docs/agentic-isj-investigation-planner.md`, plus a how-to-run placeholder noting further detail follows as agents come online.
2. **`pyproject.toml` + `uv.lock`** — own uv project, Python ≥ 3.12, deps `pydantic` and `openai`. The existing `examples/agent/` lockfile is **not** shared.
3. **`isj_agent/protocol/inp.py`** — a `pydantic.BaseModel` for the INP exactly mirroring §2.1 of the spec, including the conditionally-present slots (`nucleus`, `sub_questions`, `temporal_scope.anchor`, `fences`). Field validators for the closed enums (`question_type.primary`, `rubric.kind`, `temporal_scope.kind`).
4. **`isj_agent/agents/analyst.py`** — a stub `Analyst` class with the §4.1.1 contract:
   - takes a literal question string,
   - returns an `INP`,
   - has an `analyze(question: str) -> INP` method that raises `NotImplementedError`,
   - holds the path to its prompt file (`prompts/analyst.md`).
5. **`prompts/analyst.md`** — first-draft Analyst prompt (the §4.1.1 job description translated into a model-facing prompt). Allowed to be incomplete; will be refined in the Analyst implementation task.
6. **Python package markers** — `__init__.py` in every package directory (`isj_agent/`, `isj_agent/protocol/`, `isj_agent/agents/`, `isj_agent/engine/`, `tests/`).
7. **`.gitkeep` placeholders** — every directory in the layout that would otherwise be empty (Python directories rely on `__init__.py`; non-Python directories use `.gitkeep`). At this stage the only such directory is `tests/fixtures/`.
8. **`tests/test_analyst.py`** — a single contract-surface test that imports `Analyst`, asserts the input/output types, and asserts that `analyze()` raises `NotImplementedError`. Real behavioral tests come in the Analyst implementation task.

## What this task explicitly does NOT do

- Does not implement the Analyst's behavior (no LLM call, no prompt rendering, no actual INP production).
- Does not stub Cartographer / Strategist / Query Author / Judge / Tactician / Critic / Writer.
- Does not stub Searcher / Session Controller / Bookkeeper / Compiler / Validator.
- Does not stub the orchestrator or the CLI.
- Does not create per-topic fixtures.
- Does not touch `examples/agent/`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Directory examples/isj-agent/ exists with the subdirectory tree shown in the task description (prompts/, isj_agent/, isj_agent/protocol/, isj_agent/agents/, isj_agent/engine/, tests/, tests/fixtures/)
- [ ] #2 examples/agent/ is unmodified by this task
- [ ] #3 examples/isj-agent/README.md exists and links to docs/agentic-isj-investigation-planner.md
- [ ] #4 examples/isj-agent/pyproject.toml declares Python ≥3.12 with pydantic and openai dependencies; uv sync in examples/isj-agent/ succeeds and produces uv.lock
- [ ] #5 isj_agent.protocol.inp.INP is a pydantic model whose fields and enums match §2.1 of docs/agentic-isj-investigation-planner.md
- [ ] #6 isj_agent.agents.analyst.Analyst class exists, accepts a question string, exposes analyze(question: str) -> INP that raises NotImplementedError, and references prompts/analyst.md as its prompt path
- [ ] #7 prompts/analyst.md is present and is a first-draft Analyst prompt of at least one paragraph that references the spec
- [ ] #8 Every directory in the layout that contains no other file carries a .gitkeep (at this stage: tests/fixtures/)
- [ ] #9 Command 'uv run --project examples/isj-agent python -c "from isj_agent.agents.analyst import Analyst; from isj_agent.protocol.inp import INP"' exits 0
- [ ] #10 Command 'uv run --project examples/isj-agent pytest tests/test_analyst.py' exits 0 — the contract-surface test passes
- [ ] #11 No agent, engine module, or protocol artifact other than Analyst and INP is created with content in this task
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Create the directory tree per the layout in the description.
2. Author examples/isj-agent/pyproject.toml; run uv sync in examples/isj-agent/ to generate uv.lock.
3. Write isj_agent/protocol/inp.py — pydantic INP schema mirroring spec §2.1 (literal, compressed, question_type, nucleus, sub_questions, outline, negative_space, temporal_scope, framing_vs_content, answer_style, rubric, fences); closed-enum validators.
4. Write isj_agent/agents/analyst.py — Analyst stub class per spec §4.1.1; analyze() raises NotImplementedError.
5. Draft prompts/analyst.md — first-draft Analyst prompt translated from §4.1.1.
6. Write tests/test_analyst.py — contract-surface test (imports, types, NotImplementedError).
7. Add .gitkeep to tests/fixtures/.
8. Verify: uv run python -c 'from isj_agent.agents.analyst import Analyst; from isj_agent.protocol.inp import INP'; uv run pytest tests/test_analyst.py.
<!-- SECTION:PLAN:END -->
